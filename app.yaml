runtime: nodejs
env: flex
skip_files:
  - ^(.*/)?\DS_Store$
  - ^(.*/)?\.md$
  - ^(.*/)?\.yml$
  - ^.*\.git(/.*)?
  - ^.*node_modules(/.*)?
  - ^client(/.*)?
handlers:
  # redirect to ssl
  - url: /.*
    script: IGNORED
    secure: always

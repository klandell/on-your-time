{
  "name": "on-your-time-server",
  "version": "1.0.0",
  "description": "Server for the On Your Time subway schedule application",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "debug": "node --inspect --debug-brk ./bin/www",
    "start": "node ./bin/www",
    "build": "webpack --config ./client/webpack.config.js",
    "build:prod": "NODE_ENV=production npm run build",
    "deploy:default": "npm run build:prod && gcloud app deploy ./app.yaml --verbosity=info && gcloud app browse",
    "deploy:download": "gcloud app deploy ./download/download.yaml --verbosity=info",
    "deploy:all": "npm run deploy:download && npm run deploy:default"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/klandell/on-your-time-server.git"
  },
  "author": "Kyle Landell <kyle.landell@gmail.com>",
  "license": "GPL-3.0",
  "bugs": {
    "url": "https://github.com/klandell/on-your-time-server/issues"
  },
  "homepage": "https://github.com/klandell/on-your-time-server#readme",
  "dependencies": {
    "body-parser": "^1.16.1",
    "debug": "^2.6.1",
    "dotenv": "^4.0.0",
    "express": "^4.14.1",
    "gtfs-mongoose": "0.0.8",
    "mongoose": "^4.8.4",
    "morgan": "^1.8.1"
  },
  "engines": {
    "node": "~7.8.0"
  },
  "devDependencies": {}
}
